bound check for image accesses (textures)
doors
use the bigger map
fix a scratch in the screen
make the map transparent
refactoring
norm
memory leaks

parser
fill map width
append spaces to to make all line with the same width
door texture ob
sprite texture ob
